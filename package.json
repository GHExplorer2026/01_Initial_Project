{
  "name": "macro-events-deterministic-app",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "ESLINT_USE_FLAT_CONFIG=true eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "unit": "vitest run",
    "snapshot": "vitest run",
    "verify": "npm run unit && npm run snapshot && npm run lint && npm run typecheck && npm run build"
  },
  "dependencies": {
    "next": "^16.1.6",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "eslint": "8.57.1",
    "eslint-config-next": "^16.1.6",
    "typescript": "5.7.2",
    "vitest": "^4.0.18"
  }
}
